# =============================================================================
#  Staging — public demo applications, reached over the network.
#
#  Used by the `e2e` and `api` profiles to prove the same framework drives a
#  third-party application without a single change in the core.
# =============================================================================

baseUrl=https://www.saucedemo.com
api.baseUrl=https://dummyjson.com

credentials.username=standard_user
credentials.password=secret_sauce

# Public demo sites are slower and less reliable than the sandbox: give them air.
timeout.explicit=25
timeout.pageLoad=60
retry.count=2
